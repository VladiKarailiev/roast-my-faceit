# Get your API key at https://developers.faceit.com (Server-side key)
# DO NOT prefix with NEXT_PUBLIC_ — it must stay on the server.
FACEIT_API_KEY=

# Optional: your public site URL, used for canonical/OG tags.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
